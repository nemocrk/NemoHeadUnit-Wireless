# ---------------------------------------------------------------------------
# NemoHeadUnit-Wireless v2 — Test dependencies
# Rif: docs/TEST_SUITE_ARCHITECTURE.md §9
#
# Installazione:
#   conda activate py314
#   pip install -r v2/tests/requirements-test.txt
#
# Dipendenze già in environment.yml (non duplicate qui):
#   pyzmq, PyQt6, sounddevice, loguru, pyav, gst-libav
# ---------------------------------------------------------------------------

# Test runner
pytest>=8.0

# Coverage
pytest-cov>=5.0

# Asyncio support
pytest-asyncio>=0.23

# Timeout per test lenti / hardware
pytest-timeout>=2.3

# Property-based fuzzing
hypothesis>=6.100

# D-Bus per test bluetooth
dbus-python>=1.3

# YAML per mock_config fixture
pyyaml>=6.0
