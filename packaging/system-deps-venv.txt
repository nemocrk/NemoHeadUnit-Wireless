# packaging/system-deps-venv.txt
#
# APT system dependencies for NemoHeadUnit-Wireless when packaged with Python venv + uv.
# In addition to the base hardware/system services, this declares python3-venv,
# python3-gi, python3-dbus, and GStreamer introspection packages so they are inherited
# directly into the virtual environment via --system-site-packages.

# D-Bus & PolicyKit
dbus
polkitd

# Bluetooth stack
bluez
bluez-tools
bluez-obexd
ofono

# WiFi AP stack
hostapd
dnsmasq
iproute2
iw
rfkill

# Network Manager
network-manager

# Audio backend
pulseaudio-utils

# FFmpeg & VA-API
ffmpeg
vainfo

# GStreamer system plugins & runtime
gstreamer1.0-tools
gstreamer1.0-plugins-base
gstreamer1.0-plugins-good
gstreamer1.0-plugins-bad
gstreamer1.0-plugins-ugly
gstreamer1.0-vaapi
gstreamer1.0-gl
gstreamer1.0-qt6
gstreamer1.0-libav
gir1.2-gstreamer-1.0
gir1.2-gst-plugins-base-1.0

# Qt6 QML / Quick modules
qml6-module-qtquick
qml6-module-qtquick-controls

# GLib, GObject & D-Bus python system bindings
libglib2.0-0
python3
python3-venv
python3-gi
python3-gi-cairo
gir1.2-glib-2.0
python3-dbus

# OpenGL, EGL & Wayland runtime libraries
libgl1
libegl1
libwayland-client0
libwayland-egl1
libwayland-cursor0
libxkbcommon0

# KMS / DRM seat management for kiosk / direct boot
seatd

# Kiosk Browser fallback
surf

dmidecode